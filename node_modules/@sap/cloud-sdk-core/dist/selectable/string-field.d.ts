/*!
 * Copyright (c) 2019 SAP SE or an SAP affiliate company. All rights reserved.
 */
import { Constructable } from '../constructable';
import { EdmType } from '../edm-types';
import { Entity } from '../entity';
import { EdmTypeField, SelectableEdmTypeField } from './edm-type-field';
/**
 * Represents a property with a string value.
 *
 * @typeparam EntityT Type of the entity the field belongs to
 */
declare class StringFieldBase<EntityT extends Entity> extends EdmTypeField<EntityT, string> {
}
/**
 * Represents a selectable property with a string value.
 *
 * @typeparam EntityT Type of the entity the field belongs to
 */
export declare class StringField<EntityT extends Entity> extends StringFieldBase<EntityT> implements SelectableEdmTypeField {
    readonly selectable: true;
}
/**
 * Represents a complex type property with a string value.
 *
 * @typeparam EntityT Type of the entity the field belongs to
 */
export declare class ComplexTypeStringPropertyField<EntityT extends Entity> extends StringFieldBase<EntityT> {
    readonly parentTypeName: string;
    /**
     * Creates an instance of ComplexTypeStringPropertyField.
     *
     * @param fieldName Actual name of the field used in the OData request
     * @param entityConstructor Constructor type of the entity the field belongs to
     * @param parentTypeName Name of the parent complex type
     * @param edmType Type of the field according to the metadata description
     */
    constructor(fieldName: string, entityConstructor: Constructable<EntityT>, parentTypeName: string, edmType: EdmType);
    /**
     * Path to the field to be used in filter and order by queries. Combines the parent complex type name with the field name.
     *
     * @returns Path to the field to be used in filter and order by queries.
     */
    fieldPath(): string;
}
export {};
//# sourceMappingURL=string-field.d.ts.map