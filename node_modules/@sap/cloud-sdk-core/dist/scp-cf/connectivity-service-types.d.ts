/*!
 * Copyright (c) 2019 SAP SE or an SAP affiliate company. All rights reserved.
 */
/**
 * Proxy settings for connecting to an on-premise destination from Cloud Foundry.
 */
export interface ProxyConfiguration {
    /**
     * The host of the proxy.
     */
    host: string;
    /**
     * The port of the proxy.
     */
    port: number;
    /**
     * HTTP headers to be added to a request if tunneled through the proxy.
     * Always contains the 'Proxy-Authorization' header.
     * Also contains the 'SAP-Connectivity-Authentication' header if a JWT is present on the current request.
     */
    headers: {
        'Proxy-Authorization': string;
        'SAP-Connectivity-Authentication'?: string;
    };
}
//# sourceMappingURL=connectivity-service-types.d.ts.map